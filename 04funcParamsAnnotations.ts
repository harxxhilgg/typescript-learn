// Regular Func 

function addOne(num: number) {
  return num + 1
}

const res = addOne(68)
// console.log(res);

// Arrow func

const double = (x: number, y: number) => x + y

const res2 = double(34, 35)
console.log(res2);